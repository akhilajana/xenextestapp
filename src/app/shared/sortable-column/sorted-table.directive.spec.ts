import { SortedTableDirective } from './sorted-table.directive';

describe('SortedTableDirective', () => {
  it('should create an instance', () => {
    const directive = new SortedTableDirective();
    expect(directive).toBeTruthy();
  });
});
